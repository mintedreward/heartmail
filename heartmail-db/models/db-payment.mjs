/*
create table if not exists heartmail.payments (
    keyAlias text,
    amountUsd float,
    paymentKeyAlias text,
    createdAt timestamp,
    updatedAt timestamp,
    primary key (keyAlias)
);
*/
