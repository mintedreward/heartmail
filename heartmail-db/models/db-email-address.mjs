/*
create table if not exists heartmail.emailAddresses (
    address text,
    isEmail boolean,
    isPaymail boolean,
    isInternal boolean,
    createdAt timestamp,
    updatedAt timestamp,
    primary key (address)
);
*/
