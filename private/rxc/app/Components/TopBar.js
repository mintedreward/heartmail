import * as React from "react";
import { Appbar } from "react-native-paper";

const TopBar = () => (
  <Appbar.Header>
    <Appbar.Content title="Ryan X. Charles" subtitle="The limits are all in your head." />
  </Appbar.Header>
);

export default TopBar;