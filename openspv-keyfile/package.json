{
  "name": "@coasian/openspv-keyfile",
  "packageManager": "yarn@3.0.2",
  "type": "module",
  "scripts": {
    "clean": "rm -rf node_modules/* yarn.lock",
    "lint": "standard 'src/**/*.mjs' 'test/**/*.mjs'",
    "pretest": "yarn run lint",
    "test:mocha": "mocha",
    "test": "yarn run test:mocha",
    "prepublish": "yarn build"
  },
  "dependencies": {
    "@coasian/openspv-lib": "workspace:openspv-lib"
  },
  "devDependencies": {
    "mocha": "^8.3.1",
    "should": "^13.2.3",
    "standard": "^16.0.3"
  }
}
