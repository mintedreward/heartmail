{
  "name": "openspv-keyfile",
  "packageManager": "yarn@3.0.2",
  "type": "module",
  "scripts": {
    "clean": "rm -rf node_modules/* yarn.lock",
    "lint": "standard 'src/**/*.mjs' 'test/**/*.mjs'",
    "pretest": "# yarn run lint",
    "test:mocha": "mocha",
    "test": "yarn run test:mocha"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "openspv-lib": "workspace:*"
  },
  "devDependencies": {
    "mocha": "10",
    "should": "^13.2.3",
    "standard": "17"
  },
  "version": "1.0.0"
}
